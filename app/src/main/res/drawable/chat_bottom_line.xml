<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- This is the line -->
    <item
        android:left="0dp"
        android:right="0dp">
        <shape>
            <padding
                android:bottom="1px"
                android:left="0dp"
                android:right="0dp"
                android:top="0dp" />
            <solid android:color="@color/deliver_line" />
        </shape>
    </item>
    <!-- This is the main color -->
    <item>
        <shape>
            <solid android:color="@color/white" />
        </shape>
    </item>

</layer-list>